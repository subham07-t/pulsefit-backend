module.exports = (app, inDevMode, io, db) => {
  // io.on("connection", (socket) => {
    // app.get("/sockettesting", (req, res) => {
    //   try {
    //     socket.emit("FromAPI", { msg: "Hello world we have a new call" });
    //     res.json({ success: "Socket is delivered" });
    //   } catch (err) {
    //     res.json({ err: err });
    //   }
    // });
    // socket.emit("paymentinfo", { msg: "asdasss sds have a new call" });
    // socket.on("disconnect", () => {
    //   console.log("Client disconnected");
    // });
  // });

  // io.on("connection", (socket) => {
  //   socket.emit("paymentinfo", { msg: "Hello World" });
  //   socket.on("disconnect", () => {
  //     console.log("Client disconnected");
  //   });
  // });

  // console.log("nothing in socket file")
};
